name: Simple Workflow

on:
  workflow_dispatch:

jobs:
  first_job:
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run: echo "HELLO WORLD"

      - name: Print goodbye
        run: echo "Done_bye!"
